# Optional: enable cloud sync + accounts with Supabase (https://supabase.com).
# Without these the app runs fully on-device (local-only mode).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional: base path when hosting under a sub-folder (e.g. GitHub Pages → /Gym/)
VITE_BASE=/
